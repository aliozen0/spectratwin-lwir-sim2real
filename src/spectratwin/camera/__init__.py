"""Camera contract: intrinsics, pose sampling and world/camera transforms (SPEC-004).

Pure math, no Blender dependency. "Blender rendering" (SPEC-004 Goals) and
the visual-pilot acceptance criterion are a separate, later realization
step this package does not perform.
"""
